import start from './index.js';
import './styles.css';

//let cb = startEmojiRandom();
//setInterval(cb, 10000, true);



let cb = start({
  interval: 500,
  duration: 8000
});
//setInterval(cb, 3500, true);
